%nav{:class => 'navbar navbar-expand-lg justify-content-between navbar-dark round-bottom-corners bg-dark'}
  .navbar-brand
    = link_to t('title'), home_path, id: "logo", class: 'navbar-brand'
  .nav.navbar-nav.navbar-right
    %ul{ :class => 'navbar-nav mr-auto' }
      -if signed_in?
        / %li.nav-item= link_to t('header.debtors_list'), debtors_path, class: 'nav-link'
        %li.nav-item 
          = link_to t('header.invoices'), debts_path, class: 'nav-link'
        %li.nav-item 
          = link_to t('header.new_debtor'), new_debtor_path, class: 'nav-link'
        %li.nav-item 
          = link_to t('header.serch_debtor'), search_path, class: 'nav-link'
        / %li.nav-item= link_to t('header.log_out'), destroy_user_session_path, method: "delete", class: 'nav-link'
        %li.nav-item.dropdown
          %a{:href => '#', :class => 'dropdown-toggle nav-link', :'data-toggle' => 'dropdown' }
            = t('header.user')
            %b.caret
          %ul.dropdown-menu.bg-dark
            %li.nav-item.divider
            %li.nav-item
              = link_to t('header.update_info'), edit_user_registration_path(current_user), class: 'nav-link'
            %li.nav-item.divider
            %li.nav-item
              = link_to t('header.log_out'), destroy_user_session_path, method: "delete", class: 'nav-link'
      -else
        %li= link_to t('header.login'), new_user_session_path, class: 'nav-link' 
 