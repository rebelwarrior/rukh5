- provide(:title, t('debtors_views.search'))

= form_tag debtors_path, :method => 'get' do
  -# debtors_path might not be best?
  %p{:class => 'col-md-8_'}
    = search_field_tag :search, params[:search]
    .btn-group
      = submit_tag t('debtors_views.search'), :name => nil, :class => 'btn btn-secondary' 

-#end
