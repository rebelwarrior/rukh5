- provide(:title, t('dev_page.title'))
- require 'kramdown/converter/html'
= Kramdown::Document.new(render 'static_pages/versions' ).to_html.html_safe
= Kramdown::Document.new(render(t('dev_page.link_for_using'))).to_html.html_safe
